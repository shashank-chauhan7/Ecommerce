server:
  port: 8086

spring:
  application:
    name: payment-service
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:postgresql://postgres:5432/ecommerce_payments
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres_secret}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  kafka:
    bootstrap-servers: kafka:29092
    consumer:
      group-id: payment-service-group
      auto-offset-reset: earliest
    producer:
      acks: all
      retries: 3

stripe:
  api:
    key: ${STRIPE_API_KEY:sk_test_your-stripe-secret-key}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:whsec_your-webhook-secret}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  tracing:
    sampling:
      probability: 1.0
